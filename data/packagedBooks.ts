
import { CustomBook } from '../services/geminiService';

/**
 * This file contains the data for built-in books.
 * To add or edit content, you can modify the `content` property for each book object below.
 * To add a new book, copy an existing book object, give it a new unique `id`, `name`, and paste the new `content`.
 */
export const PACKAGED_BOOKS_DATA: CustomBook[] = [
    // The user has requested to remove the sample books.
    // This array is now empty. To add books, follow the structure:
    // { id: 'unique_id', name: 'Book Name', content: 'Full text of the book...' }
];
